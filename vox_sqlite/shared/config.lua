-- vox_sqlite configuration.
VoxSQLiteConfig = {
    -- The single database file every resource shares. Resolves to
    -- <Helix>/Persist/<worldId>/<file>. Change this to your server's db name.
    DatabaseFile = "server.db",

    -- Print a one-line readiness log on boot.
    Verbose = true,
}
